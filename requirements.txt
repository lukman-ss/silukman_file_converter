# Main application dependencies — pinned to tested versions
PySide6==6.11.0
opencv-python==4.13.0.92
Pillow==12.2.0
PyMuPDF==1.27.2.3
python-docx==1.2.0
python-pptx==1.0.2
openpyxl==3.1.5

# OCR — installed in separate .venv_ocr to isolate heavy dependencies
# paddleocr==3.5.0
# paddlepaddle==3.2.2

# Utilities
pdf2image  # optional: only needed if poppler-based rendering is used

# Build
pyinstaller==6.20.0

# Dev / test
pytest==9.0.3
